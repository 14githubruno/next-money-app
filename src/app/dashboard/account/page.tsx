import Heading from "@/components/ui/heading";
import { PAGES_TITLES } from "@/lib/constants";

export default function Account() {
  return <Heading level={1} text={PAGES_TITLES.h1.dashboardAccount} />;
}
